version: '3.7'

services:

  web:
    build:
      context: .
      dockerfile: Dockerfile
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - '.:/usr/src/app'
    ports:
      - 8001:8000
